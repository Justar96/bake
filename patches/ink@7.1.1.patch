diff --git a/build/measure-text.js b/build/measure-text.js
index 6934dc529bf94feed37afdf95633e4f36c6aad32..2406398835410afdcf91e06095ea0a2591f846d2 100644
--- a/build/measure-text.js
+++ b/build/measure-text.js
@@ -1,5 +1,6 @@
 import widestLine from 'widest-line';
-const cache = new Map();
+import TextCache from './text-cache.js';
+const cache = new TextCache();
 const measureText = (text) => {
     if (text.length === 0) {
         return {
diff --git a/node_modules/ink/build/text-cache.js b/build/text-cache.js
new file mode 100644
index 0000000000000000000000000000000000000000..9bec8c5dcd0f4b06fac2967ff247908c29694f14
--- /dev/null
+++ b/build/text-cache.js
@@ -0,0 +1,37 @@
+// Bound process-global layout caches by both entry count and retained UTF-16
+// code units. Streaming creates distinct text on every frame, including text
+// much larger than the terminal's visible window.
+const MAX_ENTRIES = 1000;
+const MAX_CODE_UNITS = 1024 * 1024;
+export default class TextCache {
+    entries = new Map();
+    size = 0;
+    get(key) {
+        const entry = this.entries.get(key);
+        if (entry === undefined) {
+            return undefined;
+        }
+        this.entries.delete(key);
+        this.entries.set(key, entry);
+        return entry.value;
+    }
+    set(key, value) {
+        const size = key.length + (typeof value === 'string' ? value.length : 0);
+        const existing = this.entries.get(key);
+        if (existing !== undefined) {
+            this.size -= existing.size;
+            this.entries.delete(key);
+        }
+        // Oversized values still render; caching is optional.
+        if (size > MAX_CODE_UNITS) {
+            return;
+        }
+        while (this.entries.size >= MAX_ENTRIES || this.size + size > MAX_CODE_UNITS) {
+            const oldest = this.entries.keys().next().value;
+            this.size -= this.entries.get(oldest).size;
+            this.entries.delete(oldest);
+        }
+        this.entries.set(key, { value, size });
+        this.size += size;
+    }
+}
diff --git a/build/wrap-text.js b/build/wrap-text.js
index 51f696c64dfefbbae23fc5d575dd17df333bca83..1cf2fae16e1282332fd7ca2a9bad7f1d050d0e9e 100644
--- a/build/wrap-text.js
+++ b/build/wrap-text.js
@@ -1,10 +1,11 @@
 import wrapAnsi from 'wrap-ansi';
 import cliTruncate from 'cli-truncate';
-const cache = {};
+import TextCache from './text-cache.js';
+const cache = new TextCache();
 const wrapText = (text, maxWidth, wrapType) => {
     const cacheKey = text + String(maxWidth) + String(wrapType);
-    const cachedText = cache[cacheKey];
-    if (cachedText) {
+    const cachedText = cache.get(cacheKey);
+    if (cachedText !== undefined) {
         return cachedText;
     }
     let wrappedText = text;
@@ -31,7 +32,7 @@ const wrapText = (text, maxWidth, wrapType) => {
         }
         wrappedText = cliTruncate(text, maxWidth, { position });
     }
-    cache[cacheKey] = wrappedText;
+    cache.set(cacheKey, wrappedText);
     return wrappedText;
 };
 export default wrapText;
